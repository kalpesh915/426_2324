<?php
    /*
        Short Hand If...Else
        if...else statements can also be written in one line, but the syntax is a bit different.

        This technique is known as Ternary Operators, or Conditional Expressions.

    */

    $ip1 = 100;

    $ans = ($ip1 >= 50) ? "True" : "False";

    echo "$ans";
?>